# Test Cases — ngx-admin Datepicker

Generated by `web-test-case-gen` on 2026-04-14.
Target: `http://localhost:4200/pages/forms/datepicker`

## Prerequisites

Start the Angular dev server:
```bash
cd backend
npm start
```
App runs at `http://localhost:4200`.

## How to Run Tests

```
Run all tests
Run the form-picker module tests
Run FLOW-DP-001 test
Run critical tests
Run negative tests
```

## File Structure

```
tests/
├── config.yaml        # Project config, modules, viewport definitions
├── test-cases.yaml    # All 31 test case definitions
├── case-summary.md    # Human-readable summary tables
└── README.md          # This file
```

## Test Case Types

| Prefix | Type | Count |
|---|---|---|
| FLOW-DP- | Complete user journey | 4 |
| LAYOUT-DP- | Responsive layout check | 3 |
| FUNC-DP- | Feature-level verification | 13 |
| NET-DP- | Network condition handling | 2 |
| ROLE-DP- | Access control | 1 |
| NEG-DP- | Error & boundary handling | 5 |
| SEO-DP- | Meta tag validation | 2 |
| LINK-DP- | External link health | 1 |

## Modules Under Test

| Module | Description |
|---|---|
| form-picker | Common single-date picker (`Form Picker` input) |
| range-picker | Start/end date range picker (`Range Picker` input) |
| minmax-picker | Constrained picker with today ±5 days boundary (`Min Max Picker` input) |

## Notes

- **Min/Max values are dynamic**: computed as `NbDateService.today() ± 5`. Update NEG tests if today's date changes significantly.
- **Auth**: App uses `NbDummyAuthStrategy` — any credentials work. The datepicker page requires no login.
- **Framework**: Angular 14 + Nebular 10. Use `nb-*` selectors for calendar elements.
